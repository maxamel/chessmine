REDIS_PASSWORD=changeme

# Uptime Kuma — dev defaults (local only, not used in prod)
KUMA_USERNAME=admin
KUMA_PASSWORD=changeme1

# Telegram intentionally left empty in dev — Kuma records alerts in the UI
# but no Telegram messages are sent.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Fixed dev-only push tokens. Safe to commit — they only reach the local
# Kuma instance on localhost:3001, never exposed externally.
KUMA_SANITY_PUSH_TOKEN=00000000-0000-0000-0000-000000000001
KUMA_BACKEND_PUSH_TOKEN=00000000-0000-0000-0000-000000000002
